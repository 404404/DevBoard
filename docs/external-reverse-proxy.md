# Reverse proxy deployment

The former Desktop/Caddy deployment guide is retired. DevBoard now runs as a single HTTP application container; there is no embedded proxy or FRP mode.

See [Docker deployment and reverse proxy](reverse-proxy.md) for Public Origin, trusted proxy CIDRs, SSE buffering/timeouts, Lucky/Nginx/Caddy examples, and the supported container port layout.

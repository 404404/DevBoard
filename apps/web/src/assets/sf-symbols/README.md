# SF Symbols assets

These PNG mask assets use Apple's SF Symbols artwork in monochrome Regular at 20 pt.
They are generated locally with AppKit's public `imageWithSystemSymbolName` API through
`scripts/generate-sf-symbol-assets.m`; do not edit the generated files by hand.

They are rendered through `SfSymbol` with CSS masking so the visible color follows
`currentColor`.
